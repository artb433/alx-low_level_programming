#include "main.h"

/**
 * _islower - find lowercase characters
 * @c: int value to be checked
 *
 * Return: 1 if true; 0 if false
 */
int _islower(int c)
{
	if (c >= 'a' && c <= 'z')
	{
		return (1);
	}
	else
	{
		return (0);
	}

	return (0);
}
