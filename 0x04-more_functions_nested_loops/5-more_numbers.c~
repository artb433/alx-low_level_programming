#include "main.h"

/**
 * more_numbers - print 0 to 14 ten times
 */

void more_numbers(void)
{
	int rows;
	int nums;

	rows = 0;

	while (rows < 10)
	{
		nums = 0;

		while (nums <= 14)
		{
			_putchar(nums);
		}

		_putchar('\n');
	}
}
