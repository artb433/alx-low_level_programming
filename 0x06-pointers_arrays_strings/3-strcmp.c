#include <string.h>

/**
 * _strcmp - compare provided strings
 * @s1: first string
 * @s2: second string
 *
 * Return: return result
 */

int _strcmp(char *s1, char *s2)
{
	int result = strcmp(s1, s2);

	return (result);
}
