#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	char output[] = "\"Programming is like building a multilingual puzzle";

	puts(output);
	return (0);
}
